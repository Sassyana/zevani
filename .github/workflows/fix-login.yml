name: ZEVANI Login Fix
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete login patch workflow disabled; login is handled directly in index-current.html."
